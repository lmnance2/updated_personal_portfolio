Drop a single MP3 here named halftime.mp3 to give party mode its audio.

The party-mode system works without it (the visual layer doesn't require audio); the file is loaded lazily and the player silently fails if it's missing.
